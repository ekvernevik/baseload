# Shared access code visitors enter on /login to reach the dashboard.
# In dev, if unset, the code "baseload" is accepted.
BASELOAD_ACCESS_CODE=change-me

# Secret used to sign the session cookie (HMAC). Required in production.
# Generate with: openssl rand -hex 32
SESSION_SECRET=
